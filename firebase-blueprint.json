{
  "entities": {
    "inventory": {
      "title": "Inventory Stock",
      "description": "Stock quantities for each product ID and bottle size",
      "type": "object",
      "properties": {
        "15ml": { "type": "number" },
        "55ml": { "type": "number" },
        "100ml": { "type": "number" },
        "updatedAt": { "type": "string" }
      },
      "required": ["15ml", "55ml", "100ml"]
    },
    "orders": {
      "title": "Store Orders",
      "description": "Customer completed orders with address, purchased items and payment details",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "orderNumber": { "type": "string" },
        "createdAt": { "type": "string" },
        "total": { "type": "number" },
        "status": { "type": "string" },
        "paymentMethod": { "type": "string" }
      },
      "required": ["id", "orderNumber", "createdAt", "total", "status", "paymentMethod"]
    }
  }
}
